# Specifying the ODBC driver, server name, database, etc. directly

from database_OOP import *
from queries import queries_class
# start = Database_OOP()
# start.establishing_connection()

execute = queries_class()
execute.choice()

